export enum PagesList {
  login = '/',
  registration = '/sign-up',
  chat = '/messenger',
  clientError = '/page-not-found',
  serverError = '/server-error',
  profile = '/settings',
  profileChange = '/settings/change-data',
  profileChangePassword = '/settings/change-password',
}
