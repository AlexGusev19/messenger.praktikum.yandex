.button {
  width: 280px;
  height: 37px;
  border: none;
  background-color: var(--primary-color);
  color: var(--background-color);
  border-radius: 8px;
  outline: none;
  cursor: pointer;
  padding: 0 12px;
  border-radius: 8px;
  text-align: center;
}
